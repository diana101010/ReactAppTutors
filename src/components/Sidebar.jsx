function Sidebar() {
  return (
    const menuConfig = [
      { icon: "U"
        name: "university"
      },
      {
        icon: "F"
        name: "faculty"
      }]
    <aside>
      <Menu items ={menuConfig} isVisible={true} />
    </aside>
  );
}
export default Sidebar;
